{
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "test": "cypress run",
    "e2e:open": "cypress open",
    "generate": "nuxt build && nuxt export",
    "generate:cached": "nuxt-netlify-cache"
  },
  "dependencies": {
    "nuxt-edge": "^2.13.1-26546421.dbecbe4a",
    "nuxt-netlify-cache": "^1.0.1"
  },
  "devDependencies": {
    "cypress": "^4.8.0",
    "netlify-plugin-cypress": "^1.3.11"
  }
}
