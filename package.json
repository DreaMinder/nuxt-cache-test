{
  "scripts": {
    "dev": "nuxt",
    "test": "cypress run",
    "e2e:open": "cypress open",
    "generate": "nuxt generate",
    "generate:cached": "nuxt-netlify-cache"
  },
  "dependencies": {
    "nuxt": "*",
    "nuxt-netlify-cache": "0.0.2"
  },
  "devDependencies": {
    "cypress": "^4.3.0",
    "netlify-plugin-cypress": "^1.3.2"
  }
}
